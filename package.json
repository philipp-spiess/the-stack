{
  "name": "the-stack-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter demo dev",
    "build": "bun run --filter the-stack build",
    "start": "bun run --filter demo start",
    "check": "tsgo --project packages/the-stack/tsconfig.json && tsgo --project packages/demo/tsconfig.json"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20251025.1",
    "oxfmt": "^0.8.0"
  }
}
