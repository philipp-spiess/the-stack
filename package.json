{
  "name": "the-stack-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter demo dev",
    "build": "bun run --filter the-stack build",
    "start": "bun run --filter demo start"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
